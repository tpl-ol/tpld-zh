{
  "name": "tpld-zh_test",
  "version": "0.1.0",
  "private": true,
  "description": "test only",
  "author": "sceext",
  "scripts": {
    "fmt": "prettier --write . && echo TODO tpl-ast-toml fmt",
    "fmt:check": "prettier --check .",
    "check": "echo TODO tpl-ast-toml tpld-check --fmt --block=tpld-zh/blocklist.txt",
    "test": "npm run fmt:check && npm run check"
  },
  "devDependencies": {
    "@tpl-ol/doc": "^0.1.0-a0.3",
    "prettier": "^2.4.1"
  },
  "engines": {
    "node": ">=16.0",
    "npm": ">=7.0"
  }
}
